platform: linux

image_resource:
  type: docker-image
  source: { repository: alpine }

inputs:
  - name: src
  - name: cardid-data

outputs:
  - name: src

run:
  path: ash
  args:
    - -c
    - |
      set -o errexit -o xtrace
      mkdir -p src/data
      cp -r cardid-data/* src/data/
